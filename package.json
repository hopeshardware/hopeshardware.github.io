{
  "name": "hopeshardware",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "balm-ui": "^8.25.0",
    "core-js": "^3.9.1",
    "fibers": "^5.0.0",
    "nuxt": "^2.15.3",
    "sass": "^1.35.1",
    "sass-loader": "^10.2.0"
  },
  "devDependencies": {
    "@nuxtjs/google-analytics": "^2.4.0",
    "push-dir": "^0.4.1"
  },
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": 3000
    }
  }
}
